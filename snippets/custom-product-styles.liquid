<style>
  /* Custom CTA for Dropshipping */
  .lxn-add-to-cart {
    background-color: #FF7A59 !important;
    color: #ffffff !important;
    font-size: 18px !important;
    font-weight: 800 !important;
    border-radius: 50px !important;
    padding: 16px 24px !important;
    min-height: 60px !important;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    box-shadow: 0 8px 24px rgba(255, 122, 89, 0.4) !important;
    transition: all 0.25s ease !important;
    border: none !important;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
    position: relative;
    overflow: hidden;
    margin-bottom: 12px;
  }
  .lxn-add-to-cart:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 28px rgba(255, 122, 89, 0.5) !important;
  }
  .lxn-add-to-cart::after {
    content: '';
  }
  
  /* Pulse animation */
  @keyframes lxn-pulse {
    0% { transform: scale(1); box-shadow: 0 0 0 0 rgba(255, 122, 89, 0.4); }
    70% { transform: scale(1.02); box-shadow: 0 0 0 10px rgba(255, 122, 89, 0); }
    100% { transform: scale(1); box-shadow: 0 0 0 0 rgba(255, 122, 89, 0); }
  }
  .lxn-add-to-cart {
    animation: lxn-pulse 2s infinite;
  }

  .lxn-cart-icon svg {
    width: 22px; height: 22px; fill: none; stroke: currentColor; stroke-width: 2.5;
  }

  /* Scarcity / Urgency Text */
  .lxn-urgency {
    color: #D32F2F;
    font-size: 14.5px;
    font-weight: 700;
    text-align: center;
    margin-bottom: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    animation: pulse-text 2s infinite;
  }
  @keyframes pulse-text {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.7; }
  }

  /* Trust Badges under button */
  .lxn-product-trust {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-top: 16px;
    margin-bottom: 16px;
    padding: 16px;
    background: #F4F9F8;
    border-radius: 12px;
    border: 1px solid rgba(15, 93, 90, 0.1);
  }
  .lxn-trust-item {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 13px;
    font-weight: 700;
    color: #0F5D5A;
    line-height: 1.2;
  }
  .lxn-trust-item svg {
    width: 24px;
    height: 24px;
    flex-shrink: 0;
    color: #1E88E5;
  }
</style>
